/**
 * Simple test utility for ColtJS
 */

define(function () {
    
    var tester = {
        one: "tester one data",
        two: "tester two data",
        three: "tester three data"
    };
    
    return tester;
    
});